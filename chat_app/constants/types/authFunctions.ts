export type SignUpModel = {
  email: string;
  name: string;
  password: string;
};
export type SignInModel = {
  email: string;
  password: string;
};
export type VerifyEmailModel = {
  email: string;
};
